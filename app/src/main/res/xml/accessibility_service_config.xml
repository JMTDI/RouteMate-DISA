<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service configuration for RouteMateAccessibilityService.

    - Monitors TYPE_WINDOW_STATE_CHANGED (foreground app changes) and
      TYPE_WINDOW_CONTENT_CHANGED (text / UI updates inside the dialer).
    - feedbackType = feedbackGeneric — no TTS or haptic feedback needed.
    - flagRetrieveInteractiveWindows — required to traverse the window tree
      and read phone number text from the dialer's EditText nodes.
    - packageNames is empty so the service receives events from all apps;
      filtering to dialer packages is done programmatically in the service.
    - canRetrieveWindowContent = true allows rootInActiveWindow access.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:canRequestFilterKeyEvents="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
